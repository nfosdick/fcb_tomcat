fcb_tomcat::linux::defaults_connectors:
  defaults:
    http-connector:
      port: '8080'
      protocol: 'HTTP/1.1'
      additional_attributes:
        redirectPort: '8443'
    https-connector:
      port: '8443'
      protocol: 'HTTP/1.1'
      additional_attributes:
        sslProtocol: 'TLS'
